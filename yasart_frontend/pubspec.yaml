name: yasart_frontend

description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  percent_indicator: ^4.2.2
  http: ^1.4.0
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.11
  flutter_spinkit: ^5.1.0
  serial_port_win32: ^0.1.2
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/YASARTLogo.jpg
    - assets/logo.jpg
